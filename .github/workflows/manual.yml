name: "Check actions work for issue"
on: workflow_dispatch

jobs:
  create-issue:
    runs-on: ubuntu-latest
    steps:
      - uses: rerost/issue-creator@master
        with:
          template-issue: 1
